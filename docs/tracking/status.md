# 현재 개발 상태

최종 갱신: 2026-09-17, HEAD 커밋 `4ddd1cf` 기준 origin/main을 병합

이 문서는 **지금 시점의 사실만** 담는다. 세션별로 무엇을 어떻게 검증했는지의 서술은 이 문서에 더 쌓지 않고 git 커밋 로그(`git log`)와 커밋 메시지로 남긴다. 과거의 서술형 기록이 필요하면 해당 커밋을 찾아본다.

## 최근 자동 검사 상태

병합 직후 아래를 다시 실행해 모두 통과했다(정확한 테스트 개수는 이 세션의 커밋 메시지 참고).

- Vitest 단위 테스트
- Next.js 경로 타입 생성과 TypeScript 검사
- ESLint 전체 검사: 오류 0개, 기존 이미지 경고 2개(Markdown 이미지 렌더링의 `<img>` 사용)
- Next.js 프로덕션 빌드
- 로컬 Supabase SQL 테스트: `ai_settings.sql`·`lint_suggestions.sql`·`document_wiki_links.sql`·`document_crud.sql`·`document_lifecycle.sql`·`annotations.sql`·`search.sql` 통과. `rls.sql`은 픽스처 이메일이 로컬 개발 계정과 겹쳐 실패(코드 변경과 무관, 픽스처 이메일 교체로 해결 가능)
- 이메일/비밀번호 로그인·로그아웃·틀린 비밀번호 오류, 239개 문서 ZIP의 분석→커밋 왕복과 재시도 시 건너뛰기를 로컬과 실제 프로덕션(Vercel+Supabase) 양쪽에서 확인(커밋 `d90731a`)

## 구현되어 있고 검증된 범위

- 소유자 이메일 한 명을 허용하는 이메일/비밀번호 로그인(2026-09-17 이전엔 매직 링크였음)과 안전한 콜백 경로(현재는 미사용, 소셜 로그인 추가 시 재사용 목적으로 보존)
- 사용자 ID 기반 스키마와 주요 테이블·Storage의 행 단위 권한 정책
- 문서 생성·수정, 명시적 저장, 리비전, 버전 충돌, 휴지통 이동·복원
- 제목·별칭, 명시적 Wiki link, 백링크와 한글 문서 주소
- Markdown 렌더링, 원시 HTML 차단, 외부 링크 보호, 줄바꿈 선택
- 호버 미리보기, 클릭 고정 카드(최대 3개, 4번째부터 가장 오래된 카드를 탐색 경로로 접음), 카드 안 연결 탐색, 이미지 확대 보기, 카드가 열리고 닫힐 때 포커스 이동·Escape 복귀(키보드·스크린리더 접근), 고정 카드는 fetch 완료 전 스켈레톤을 먼저 보여줌
- 제목·별칭·사용자에게 보이는 본문 검색(`documents.search_text` 생성 열 + trigram 인덱스), 평문 문맥 미리보기, 최근 검색어, 실제 열람 기반 최근 문서, 검색 결과가 상한(명령 팔레트 20개·전체 검색 30개)에 닿으면 안내 표시
- 이미지 업로드·붙여넣기(서식 있는 텍스트 붙여넣기 포함), 비공개 다운로드, 내용 해시 기반 중복 제거
- 저장되지 않은 임시 문서·고아 첨부파일 정리(`/settings/drafts`): 30일 유예, AI 답변 초안 제외, Storage와 DB 일관 삭제(수동 실행)
- `.md`/`.markdown`/`.txt` 다중 파일 가져오기, ZIP·Obsidian vault 가져오기(경로 이탈·심볼릭 링크·압축 폭탄 방어, 묶음 전체 카탈로그 확정 후 링크 재색인), 제목·별칭 외 알 수 없는 frontmatter 필드 보존
- 가져오기 진행률: `GET /api/imports/[jobId]` 상태 폴링, 60초 함수 타임아웃 시 자동 이어하기(최대 8회, 잡 큐 없음), 페이지 이탈에도 유지되는 전역 하단 상태 표시줄, 낮춘 ZIP 상한(문서 150개·50MB)으로 재시도 자체를 최소화
- 로딩 스켈레톤: 공용 `Skeleton` 컴포넌트, 문서 목록·검색 결과 `loading.tsx`(같은 레이아웃 안 세그먼트 이동에만 적용, `(wiki)` 레이아웃이 런타임 데이터를 쓰므로 첫 하드 리로드에는 미적용), 위키링크 호버·클릭 미리보기 카드의 즉시 로딩 표시
- 전체 내보내기(`/api/export`, 동기 방식): Markdown·frontmatter·별칭·첨부·`manifest.json`·`index.md`를 담은 ZIP, 가져오기 왕복 호환성 확인됨(완전히 빈 환경으로의 복원은 미확인)
- 로고 적용(파비콘·README·앱 헤더), 사이드바 최근 문서 제목 말줄임·날짜 그룹·커서 기반 무한 스크롤, 문서 본문 목차 자동 생성과 클릭 이동
- 테마·명암·글꼴·크기·본문 폭·미리보기 설정과 새로고침 후 유지
- 정리 제안(미해석 링크, 고아·깨진 링크, 정방향 링크, 빈 요약 제안, 승인·무시, 다시 색인)
- AI BYOK: 공급자 추상화(Anthropic·OpenAI), 키 암호화 저장·검증·삭제·월 한도, 고정 카드 안 질문(스트리밍)과 답변의 초안 문서 저장, 파생 불가 문서의 AI 요약 제안
- 문서 개인 주석 기능은 애플리케이션에서 제거됨(기존 Supabase 테이블·함수·데이터는 보존)

## 배포

2026-09-17에 Personal MVP 배포 대상을 Vercel(무료 Hobby 플랜)과 Supabase(서울 리전, 무료 티어)로 정했다. `main` push마다 Vercel Git 연동으로 자동 배포된다. 자세한 절차와 서버리스 함수 리전·시간제한 제약은 [operations.md](../operations.md#배포) 참고.

## 남은 작업 순서

1. 사이드바를 옵시디언처럼 여러 패널을 전환할 수 있는 구조로 확장, 문서 전체 목록 패널부터.
2. AI 후속(선택): 암호화 비밀 로테이션 스크립트, 깨진 링크 초안·모순 검사·편집기 diff 제안, 공급자 추가(Gemini).
3. `tracking/proposals/ux-ia-diagnostic-report-2026-09-16.md`의 P2 이하 항목 채택 여부 결정(P0/P1은 반영됨).

내보내기 왕복은 같은(비어 있지 않은) 로컬 환경에 다시 올려 제목 충돌로 판정되는 것까지만 확인했고, 완전히 빈 환경에 복원하는 것은 아직 확인하지 않았다.

## 알려진 미해결 항목

- 가져오기 스테이징 버킷(`data-jobs`)과 ZIP 원본·분석 보고서를 7일 뒤 지우는 주기 작업이 없음 — 상세는 `findings.md` 참고.
- 커밋 재시도마다 소유자 전체 문서를 다시 재색인함(대량 가져오기 재시도 시 비용 증가) — 상세는 `findings.md` 참고.
- 로그인·새로고침 직후 `listDocuments`가 간헐적으로 실패하는 현상이 재현 없이 두 번 목격됨 — 상세는 `findings.md` 참고.
- 목차 배치 방식과 frontmatter 정보 카드·태그 칩은 `tracking/proposals/wiki-interface-proposal.md`에 제안만 있고 결정되지 않음.

## 후속 범위 (아직 착수 안 함)

- 모바일에서 카드 탐색을 동일하게 사용할 수 있는 하단 시트·키보드·스크린리더 경험
- 가져오기 대량 배치의 진짜 비동기 처리(현재는 60초 함수 제한을 같은 요청 재시도로 우회하는 임시방편), 정기 백업과 복원 리허설, 감사 기록과 운영 알림
- 개인 사용 경험 검증 후 일반 가입·계정 복구·탈퇴·악용 방지를 포함한 다중 사용자 서비스 전환
- 사용자가 선택한 문서를 명시적으로 공유하거나 게시하는 기능

협업 편집, 추천 피드, 그래프 화면, 플러그인 생태계는 핵심 기록·탐색·이식 경험이 검증된 뒤 평가한다. AI는 설계 13절의 제안 방식(BYOK, 원문 자동 수정 없음)으로만 도입한다.
